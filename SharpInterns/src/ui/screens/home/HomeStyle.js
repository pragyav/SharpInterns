import {
    StyleSheet
} from 'react-native';
import AppTheme from "../../theme/AppTheme";

const HomeStyle = StyleSheet.create({
    container: {
        flex: 1
    },
});

export default HomeStyle;